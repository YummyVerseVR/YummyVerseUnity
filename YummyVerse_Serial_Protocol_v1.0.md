# YummyVerse 咀嚼計シリアル通信プロトコル仕様書

| 項目 | 値 |
|---|---|
| 文書ID | `YV-SERIAL-001` |
| プロトコルバージョン | `1` |
| 文書バージョン | `1.0` |
| 状態 | 合意済み仕様 |
| 更新日 | 2026-08-29 |

## 1. 目的と適用範囲

本仕様は、YummyVerseのUnity製PCVRアプリケーションと、開口・閉口を検知する咀嚼計との間で使用する有線シリアル通信を定義する。

- キャリブレーション制御は要求・応答型とする。
- 開閉通知は低遅延を優先した一方向イベント型とする。
- マイコン、開発ボード、USB-UART変換IC、VID/PIDには依存しない。
- PC上でCOMポートを提供し、本仕様の通信設定とメッセージを実装するデバイスを適合デバイスとする。

センサーの物理構造、開閉判定アルゴリズム、咀嚼音の選択方法およびVR表現は本仕様の対象外とする。

## 2. 設計原則

1. デバイスの確定判定には`HELLO` / `READY`ハンドシェイクを使用し、VID/PIDを必須条件にしない。
2. COMポート探索とYummyVerseメッセージ処理を分離し、マイコン変更がUnity側のプロトコル実装へ波及しないようにする。
3. キャリブレーション要求はリクエストIDで対応付け、重複送信に対して冪等に処理する。
4. 開閉イベントには連番、ACK、再送および欠落回復を設けず、受信時点で即時処理する。
5. シリアル通信をバイトストリームとして扱い、LFによってメッセージ境界を確定する。

## 3. 役割

| 名称 | 役割 |
|---|---|
| Unity | COMポート探索、ハンドシェイク、キャリブレーション開始、開閉イベントの消費を担当する。 |
| 咀嚼計 | ハンドシェイクへの応答、キャリブレーション、開閉イベントの発行を担当する。 |
| トランスポート層 | PC上のCOMポートと、その背後にあるUSB CDC、USB-UART等。具体的なマイコンやUSB実装は問わない。 |
| アプリケーション層 | 本仕様で定義するLF終端のYummyVerseメッセージ。デバイス判定はこの層で行う。 |

## 4. シリアル通信設定

| 項目 | 規定値 |
|---|---|
| 接続形態 | PCからCOMポートとしてアクセスできる有線シリアル接続 |
| ボーレート | 115200 baud |
| データビット | 8 bit |
| パリティ | なし |
| ストップビット | 1 bit |
| フロー制御 | なし |
| 文字集合 | ASCII範囲のUTF-8 |
| メッセージ終端 | LF（`0x0A`） |

USB CDCではボーレート設定が物理伝送速度に直接影響しない実装もあるが、Unityと咀嚼計は互換性のため同じ規定値を受け入れなければならない。

## 5. メッセージ構文

1メッセージは、カンマ区切りのフィールド列と終端LFから構成する。

```text
FIELD_0,FIELD_1,...,FIELD_N\n
```

- 命令名と列挙値はASCII大文字とする。
- フィールド前後に空白を入れない。
- 受信側はCRLFを受けた場合、LF直前のCR（`0x0D`）を除去して解釈してよい。
- メッセージ本文はLFを除いて63バイト以下とする。
- フィールド値にカンマ、CRまたはLFを含めてはならない。
- 空行は無視する。
- 命令名および列挙値は大文字・小文字を区別する。
- 受信側はLFまでデータを蓄積し、1回のRead操作を1メッセージと仮定してはならない。
- 本バージョンではCRCを付与しない。
- 不正な形式は破棄し、必要に応じて受信側のローカルログへ記録する。

## 6. COMポート自動探索

UnityはCOMポート番号を事前固定せず、アクセス可能なポートを列挙して、各ポートへ個別に識別要求を送る。

シリアル通信には物理的なブロードキャストがないため、これはUnityが実行する疑似ブロードキャストである。

### 6.1 探索手順

1. アクセス可能なCOMポートを列挙する。
2. 候補ポートを本仕様の通信設定で開く。
3. `HELLO,YUMMYVERSE,1`を送信する。
4. `READY,YUMMYVERSE,1,CHEWING_SENSOR`を受信したポートを適合デバイスとして採用する。
5. 正しい応答が得られなかったポートを閉じ、次の候補を試す。

### 6.2 判定規則

- VID/PID、製品名、マイコン名、前回のCOM番号は、探索順の最適化に使用してよい。
- 上記情報を理由にポートを恒久的に除外してはならない。
- 最終判定は必ず`READY`メッセージで行う。
- ハンドシェイク成功前は、`HELLO`以外のYummyVerse命令を送信してはならない。
- 複数の適合デバイスを検出した場合、プロトコルバージョン1ではUnityが使用するCOMポートをユーザーに選択させる。

> 任意のシリアルデバイスへ対応しつつ自動検出するには、Unityが正体不明のCOMポートへ`HELLO`を送る必要がある。無関係な機器への影響を抑えるため、ハンドシェイク成功前に送る命令を`HELLO`だけに制限し、応答しないポートは速やかに閉じる。

## 7. ハンドシェイク

```text
Unity  -> 咀嚼計 : HELLO,YUMMYVERSE,1\n
咀嚼計 -> Unity  : READY,YUMMYVERSE,1,CHEWING_SENSOR\n
```

| フィールド | 意味 |
|---|---|
| `HELLO` / `READY` | 識別要求と応答。リクエストIDは使用しない。 |
| `YUMMYVERSE` | 他のシリアル機器と区別するプロトコル識別子。 |
| `1` | 本仕様のプロトコルバージョン。 |
| `CHEWING_SENSOR` | YummyVerse内でのデバイス役割。 |

### 7.1 ハンドシェイクの規則

- `HELLO`は冪等である。
- 咀嚼計は同じ`HELLO`を複数回受信しても、通信状態やキャリブレーション状態を変更しない。
- 咀嚼計は正しい`HELLO`を受信するたびに同じ`READY`を返す。
- `READY`は通信プロトコルへ応答可能であることを示す。キャリブレーションが未実行であることまでは保証しない。
- Unityは、ポートオープン時のデバイスリセットなどによって最初の`HELLO`が失われる可能性を考慮して再送する。
- 咀嚼計は起動時に自発的な`READY`を送ってもよい。
- Unityは自発的な`READY`だけに依存せず、必ず`HELLO`を送信して接続を確立する。
- DTR/RTSによるリセットの有無はデバイス固有の実装事項であり、プロトコル成立の前提にしない。

## 8. メッセージ一覧

| 方向 | 形式 | 意味 | 配送方針 |
|---|---|---|---|
| Unity → 咀嚼計 | `HELLO,YUMMYVERSE,1` | 咀嚼計の識別要求 | 再送可 |
| 咀嚼計 → Unity | `READY,YUMMYVERSE,1,CHEWING_SENSOR` | 識別応答 | `HELLO`ごとに応答 |
| Unity → 咀嚼計 | `CAL_START,<requestId>` | キャリブレーション開始要求 | 同じIDで再送可 |
| 咀嚼計 → Unity | `CAL_ACCEPTED,<requestId>` | 要求受理・処理開始 | 要求応答 |
| 咀嚼計 → Unity | `CAL_DONE,<requestId>` | キャリブレーション正常完了 | 終端応答 |
| 咀嚼計 → Unity | `CAL_FAILED,<requestId>,<reason>` | 要求拒否または処理失敗 | 終端応答 |
| 咀嚼計 → Unity | `MOUTH,OPEN` | 開口イベント | ACK・再送なし |
| 咀嚼計 → Unity | `MOUTH,CLOSED` | 閉口イベント | ACK・再送なし |

## 9. キャリブレーション

### 9.1 正常シーケンス

```text
Unity  -> 咀嚼計 : CAL_START,42\n
咀嚼計 -> Unity  : CAL_ACCEPTED,42\n
咀嚼計 -> Unity  : CAL_DONE,42\n
```

- `CAL_ACCEPTED`は命令を受信して処理を開始したことを示す。
- `CAL_DONE`はキャリブレーション処理そのものが完了したことを示す。
- Unityは同時に1件だけキャリブレーション要求を保留できる。
- 咀嚼計はキャリブレーション中に`MOUTH`イベントを送信しない。

### 9.2 失敗シーケンス

```text
Unity  -> 咀嚼計 : CAL_START,43\n
咀嚼計 -> Unity  : CAL_ACCEPTED,43\n
咀嚼計 -> Unity  : CAL_FAILED,43,SENSOR_UNSTABLE\n
```

`CAL_ACCEPTED`後であっても、測定値が安定しない等の理由で失敗し得る。`CAL_DONE`と`CAL_FAILED`は相互排他的な終端応答であり、同一要求に対して両方を送信してはならない。

### 9.3 重複要求

- 同じ`requestId`を実行中に再受信した場合、キャリブレーションを再開せず`CAL_ACCEPTED`を再送する。
- 同じ`requestId`の処理が完了済みの場合、直前の`CAL_DONE`または`CAL_FAILED`を再送する。
- 異なる`requestId`を実行中に受信した場合、`CAL_FAILED,<newRequestId>,BUSY`を返す。
- 重複判定用の結果保持は、同一デバイス起動中の直近要求について行えばよい。
- デバイス再起動後まで結果を保持する必要はない。

## 10. リクエストID

| 項目 | 規則 |
|---|---|
| 型 | 32ビット符号なし整数（Unity: `uint`、デバイス: `uint32_t`相当） |
| 有効範囲 | `1`～`4,294,967,295` |
| 予約値 | `0`は「保留要求なし」等の内部表現用として予約 |
| 発行元 | Unity |
| 折り返し | `4,294,967,295`の次は`1` |
| 比較 | 一致判定だけを使用し、大小比較で新旧を判断しない |

Unity側の発番例：

```csharp
nextRequestId = currentRequestId == uint.MaxValue
    ? 1u
    : currentRequestId + 1u;
```

Unityは新しい接続を確立した際、以前の接続で保留していたリクエストを失敗として終了させる。古い接続の応答を新しい要求へ関連付けてはならない。

## 11. 開閉イベント

```text
咀嚼計 -> Unity : MOUTH,OPEN\n
咀嚼計 -> Unity : MOUTH,CLOSED\n
```

開閉イベントは咀嚼音再生のトリガーであり、最新状態を同期するための状態スナップショットではない。

- イベント連番を付けない。
- UnityはACKを返さない。
- 咀嚼計は再送しない。
- 欠落検出および欠落イベントのリカバリーを行わない。
- Unityは`OPEN`と`CLOSED`の交互性を検証しない。
- `OPEN → OPEN`または`CLOSED → CLOSED`も独立したイベントとして処理する。
- Unityはイベント受信時点で対応する咀嚼音を再生する。
- 切断または再接続時、未処理の開閉イベントと部分受信中の行を破棄する。
- 咀嚼計はキャリブレーション中に`MOUTH`イベントを送信しない。

## 12. エラー理由

| `reason` | 意味 |
|---|---|
| `BUSY` | 別のキャリブレーション要求を実行中 |
| `SENSOR_ERROR` | センサー値を正常に取得できない |
| `SENSOR_UNSTABLE` | 測定値が安定せずキャリブレーションできない |
| `INTERNAL_ERROR` | 上記以外の内部エラー |

形式不正、未知の命令、範囲外の`requestId`など、要求IDを安全に特定できない入力に対して`CAL_FAILED`を返してはならない。該当行を破棄し、必要に応じて受信側のローカルログへ記録する。

## 13. タイムアウトと再送

以下は相互運用のための推奨初期値である。センサー実装上の最大キャリブレーション時間が判明した場合は、その値を優先して調整する。

| 対象 | 推奨初期値 | タイムアウト時の処理 |
|---|---:|---|
| `HELLO`再送間隔 | 500 ms | 同じ`HELLO`を再送する |
| 候補ポートの探索期限 | 5 s | ポートを閉じて次の候補へ進む |
| `CAL_ACCEPTED`待機 | 1 s | 同じ`requestId`の`CAL_START`を再送する |
| `CAL_DONE` / `CAL_FAILED`待機 | 最大処理時間＋安全余裕 | 要求失敗としてUIへ通知する |

`CAL_START`を再送する場合は、新しい`requestId`を発行してはならない。新しいIDを発行すると、咀嚼計が元の要求を受理済みだった場合に二重要求となる。

## 14. 切断と再接続

1. UnityはI/O例外、ポート消失または明示的な切断を検出したら、そのポートを閉じる。
2. 部分受信行、未処理の開閉イベントおよび保留中のキャリブレーション要求を破棄する。
3. COMポート探索へ戻り、`HELLO` / `READY`ハンドシェイクを最初から実行する。
4. 再接続後、必要であればUnityの操作によってキャリブレーションを改めて開始する。

COMポート番号は再接続後に変化する可能性がある。前回成功した番号は探索優先度にのみ使用し、その番号へ固定してはならない。

## 15. 受信実装要件

### 15.1 Unity

- ブロッキングする`ReadLine`等をUnityメインスレッドから直接呼び出さない。
- 受信は専用スレッドまたは非同期処理で行う。
- 解析済みイベントは、スレッドセーフなキューを介してメインスレッドへ渡す。
- 開閉イベントは蓄積遅延を避けるため速やかに消費する。
- 切断時には未処理キューを破棄する。
- 未知・不正な行によって受信ループ全体を停止させない。

### 15.2 咀嚼計

- 受信したバイトをLFまで蓄積し、完全な行だけを解析する。
- 本文が63バイトを超えた場合、次のLFまで破棄して同期を回復する。
- `HELLO`への応答をキャリブレーション処理より優先し、速やかに`READY`を返す。
- センサー判定では必要なチャタリング除去またはヒステリシスを実装する。

## 16. 状態遷移

| 主体 | 現在状態 | 入力・事象 | 次状態 / 動作 |
|---|---|---|---|
| Unity | 未接続 | COMポート検出 | 探索中：ポートを開いて`HELLO`を送信 |
| Unity | 探索中 | 正しい`READY` | 接続済み：対象ポートを採用 |
| Unity | 接続済み | キャリブレーション開始操作 | 処理待ち：`CAL_START`を送信 |
| Unity | 処理待ち | `CAL_DONE` / `CAL_FAILED` | 接続済み：結果を反映 |
| Unity | 任意 | 切断 | 未接続：保留データを破棄して再探索 |
| 咀嚼計 | 待機 | `CAL_START` | 実行中：`CAL_ACCEPTED`を返信 |
| 咀嚼計 | 実行中 | 完了 / 失敗 | 待機：`CAL_DONE`または`CAL_FAILED`を返信 |
| 咀嚼計 | 待機 | 開閉検出 | 待機：`MOUTH`イベントを送信 |

## 17. 完全な通信例

```text
咀嚼計 -> Unity : READY,YUMMYVERSE,1,CHEWING_SENSOR\n
                  # 起動時の任意通知。Unityはこれだけには依存しない。

Unity  -> 咀嚼計 : HELLO,YUMMYVERSE,1\n
咀嚼計 -> Unity  : READY,YUMMYVERSE,1,CHEWING_SENSOR\n

Unity  -> 咀嚼計 : CAL_START,42\n
咀嚼計 -> Unity  : CAL_ACCEPTED,42\n
咀嚼計 -> Unity  : CAL_DONE,42\n

咀嚼計 -> Unity  : MOUTH,OPEN\n
咀嚼計 -> Unity  : MOUTH,CLOSED\n
```

## 18. 適合確認チェックリスト

- [ ] マイコンやVID/PIDに依存せず、正しい`READY`だけでデバイスを確定できる。
- [ ] Unityと咀嚼計が115200・8N1・LF終端を使用している。
- [ ] 分割受信と複数行の一括受信の両方を正しく処理できる。
- [ ] `HELLO`の重複受信が副作用を起こさない。
- [ ] `CAL_START`の重複受信でキャリブレーションが二重実行されない。
- [ ] リクエストIDの最大値から1への折り返しで大小比較を使用していない。
- [ ] キャリブレーション中に`MOUTH`イベントを送信しない。
- [ ] Unityが開閉イベントの交互性、ACK、再送または欠落回復を要求していない。
- [ ] 切断時に部分行、未処理イベントおよび保留要求を破棄できる。
- [ ] UnityメインスレッドをシリアルI/Oでブロックしない。

## 19. バージョン1で定義しない事項

- 開閉イベントの連番、ACK、再送、欠落回復および状態同期
- 複数咀嚼計の一意な個体識別子
- CRCまたはメッセージ認証
- VID/PIDによる必須ホワイトリスト
- シリアル以外のBluetooth・ネットワーク通信
